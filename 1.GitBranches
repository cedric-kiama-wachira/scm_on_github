git checkout -b devone

Switched to a new branch 'devone'

git checkout -b Master

Switched to a new branch 'Master'

git commit -am "Message Name"

git log --graph --pretty --decorate --name-only 


# Merging - Fast foward(WHen the main branch has no coommits) and No Fast Foward(Commin case where the main branch will always have a commit)
git checkout master 

git merge branch/name

